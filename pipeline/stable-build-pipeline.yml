trigger:
  branches:
    include:
    - refs/tags/*
pool:
  vmImage: 'ubuntu-latest'

extends:
  template: common-job-template.yml
  parameters:
    addon: 'zigbee2mqtt'
    extra_param: '--no-cache --docker-hub-check'
